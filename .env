# --- App Settings ---
APP_NAME=conduit
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8000
JWT_SECRET=supersecretkeyyoushouldnotcommittogithub

# --- Database Settings (para Docker) ---
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=conduit
DB_USERNAME=conduit_user
DB_PASSWORD=secret

# --- Analytics (read-only) DB user for Python backend ---
# These credentials will be used by the Python analytics service to connect
# as a read-only user. Create the user in MySQL using the SQL provided in the
# project README or below.
ANALYTICS_DB_HOST=host.docker.internal
ANALYTICS_DB_USERNAME=analytics
ANALYTICS_DB_PASSWORD=analytics_pass
ANALYTICS_DB_PORT=33067


